# Browser QA captures

These reviewed desktop and mobile captures document the public repository's
initial visual state. New browser-QA captures are written to the ignored
`test-results/visuals/` directory so routine checks do not modify tracked files.
